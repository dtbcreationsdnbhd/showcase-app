export default function Home() {
  return (
    <main style={{ padding: "3rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>showcase-app Admin</h1>
      <p>Default Next.js stub. Business features come in a later phase.</p>
      <p>
        Dev server: <code>pnpm dev:admin</code> → http://localhost:3001
      </p>
    </main>
  );
}
